---
apiVersion: v1
kind: BuildConfig
metadata:
  name: kopf-simple
spec:
  strategy:
    sourceStrategy:
      from:
        # Replace this with the built image name
        name: "{{ REPLACE_IMAGE }}"
